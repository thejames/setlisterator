# Copy to .env and fill in. The script reads these via python-dotenv.
SETLISTFM_API_KEY=your-setlistfm-api-key
PLEX_BASEURL=http://localhost:32400
PLEX_TOKEN=your-plex-token
PLEX_MUSIC_LIBRARY=Music

# Web app only. Port for `python web.py` (5000 is taken by AirPlay on macOS).
PORT=5001

# Optional. Default setlist.fm username, pre-filled on the web Attended page.
SETLISTFM_USER=

# Optional. YouTube Music OAuth file for building playlists there (web app).
# Generate it with `ytmusicapi oauth` and point this at the result; defaults to
# ytm_oauth.json beside history.json. Leave unset to keep YTM disabled.
YTM_OAUTH_FILE=
